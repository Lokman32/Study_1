export default class chambre {
  constructor(code, nblits, s, e, desc) {
    this.code = code;
    this.nombreLits = nblits;
    this.surface = s;
    this.etage = e;
    this.description = desc;
  }
}
